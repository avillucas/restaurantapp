import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ar.lodetito.app',
  appName: 'restaurantApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
